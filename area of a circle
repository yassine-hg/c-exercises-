#include <stdio.h>
#include <math.h>

int main() {
    int ray ;

    printf("donne mois le rayon : ");
    scanf("%d" , &ray);

    int power = pow(ray , 2);

    float rayon = 3.14 * power ;

    printf("la surface du cercle est : %.1f\n" , rayon);

    return 0;

}
