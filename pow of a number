#include <stdio.h>
#include <math.h>

int main() {
    int num ;

    printf("Enter a number : ");
    scanf("%d" , &num);

    int power = pow(num , 2 );

    printf("le double est : %d" , power);

    return 0;
}
